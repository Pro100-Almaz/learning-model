MATH CONSTRAINTS (do not alter any number):
- Arithmetic progression with first term a_1 = {{ a1 }} and common difference d = {{ d }}.
- Task: find the {{ n }}-th term and the sum of the first {{ n }} terms.
- Render all math as LaTeX inside $...$ (e.g. $a_{{ '{' }}{{ n }}{{ '}' }}$ and $S_{{ '{' }}{{ n }}{{ '}' }}$).
- Do NOT state or hint at the final numeric values.
