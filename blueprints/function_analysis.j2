GRAPH CONSTRAINTS (do not alter any number):
- A graph of some function y = f(x) is given with the following key features:
  - crosses the x-axis (zeros) at x = {{ zero_1 }} and x = {{ zero_2 }};
  - has a local maximum at the point x = {{ x_max }};
  - has a local minimum at the point x = {{ x_min }}.
- Task: read the graph and describe the function by the 7-step algorithm — domain,
  range, zeros, parity, monotonicity, sign-constancy, and extremum points.
- The shape is: rising up to the maximum at x = {{ x_max }}, falling between the
  maximum and the minimum at x = {{ x_min }}, then rising again after the minimum.
- The graph has no axis-of-symmetry ("бабочка") nor central symmetry, so the function
  is of general type (общего вида).
- Render coordinates and points as LaTeX inside $...$ (e.g. $x = {{ zero_1 }}$, $x = {{ x_max }}$).
- Do NOT state or hint at the answer: the zeros, monotonicity intervals, sign intervals,
  parity classification, or extremum points must NOT be listed for the student.
