MATH CONSTRAINTS (do not alter any number):
- Function: f(x) = {{ a }}x^2 + {{ b }}x + {{ c }}
- Task: compute the area under f(x) between x = {{ lower }} and x = {{ upper }}.
- The student must set up and evaluate the definite integral of f(x) from {{ lower }} to {{ upper }}.
- Render all math as LaTeX inside $...$ (e.g. $\int_{{ '{' }}{{ lower }}{{ '}' }}^{{ '{' }}{{ upper }}{{ '}' }} f(x)\,dx$).
- Do NOT state or hint at the final numeric answer.
