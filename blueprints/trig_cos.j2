MATH CONSTRAINTS (do not alter any number):
- Function: y = cos(x)
- Task: state the basic properties of the cosine function from its graph — domain,
  range, parity (even/odd), and period — and describe its interval of decrease
  starting from x = {{ interval_start | replace('*', '') }}.
- Key facts to recall (do NOT reveal them as the answer): the cosine is defined for
  all x, its graph stays within a bounded horizontal strip, it is symmetric about the
  OY axis (the one even basic trig function), and its wave repeats after a fixed
  interval; it decreases over one half-period and rises over the other.
- Render all math as LaTeX inside $...$
  (e.g. $y = \cos x$ and $x = {{ interval_start | replace('*', '') | replace('pi', '\\pi') }}$).
- Do NOT state or hint at the answer: the domain, range, parity, period, or which
  intervals are decreasing must NOT be given to the student.
