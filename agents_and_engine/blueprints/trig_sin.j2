{%- set innerLatex = 'x' if freq == 1 else (freq ~ 'x') -%}
PROBLEM TO POSE (turn ONLY this into the word problem; keep every number exactly as written):
- Function: y = sin({{ innerLatex }})
- Ask the student to state the basic properties of this sine function from its graph —
  domain, range, parity (even/odd), and period.

=== INTERNAL (guidance for you only — never show, restate, paraphrase, number, or hint any of this to the student) ===
- Key facts to recall: the sine is defined for all x, its graph never leaves a bounded
  horizontal strip, it is symmetric about the origin, and its wave repeats after a fixed
  interval; a coefficient k inside the function compresses the graph, dividing the period
  by k.
- Render all math as LaTeX inside $...$ (e.g. $y = \sin\left({{ innerLatex }}\right)$).
- Do NOT state or hint at the answer: the domain, range, parity, or period must NOT be
  given to the student.
