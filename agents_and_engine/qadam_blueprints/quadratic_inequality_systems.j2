{% macro poly(a, b, c) -%}
{%- if a == 1 -%}x^2
{%- elif a == -1 -%}-x^2
{%- else -%}{{ a }}x^2
{%- endif -%}
{%- if b > 0 %} + {% if b != 1 %}{{ b }}{% endif %}x
{%- elif b < 0 %} - {% if b != -1 %}{{ -b }}{% endif %}x
{%- endif -%}
{%- if c > 0 %} + {{ c }}
{%- elif c < 0 %} - {{ -c }}
{%- endif -%}
{%- endmacro %}

PROBLEM TO POSE (turn ONLY this into the word problem; keep every number exactly as written):
- Теңсіздіктер жүйесін шешіңіз:
  $\begin{cases}
  {{ poly(a1, b1, c1) }} {{ relation_1_latex }} 0,\\
  {{ poly(a2, b2, c2) }} {{ relation_2_latex }} 0.
  \end{cases}$
- Жауапты аралықтар түрінде жазыңыз.

=== INTERNAL (guidance for you only — never show, restate, paraphrase, number, or hint any of this to the student) ===
- Solve each quadratic inequality separately using the interval/sign-chart method.
- Put every real zero on its number line in increasing order and determine the sign on each interval.
- Respect strict versus non-strict inequalities when deciding whether a zero is included.
- The system answer is the intersection of the two individual solution sets, not their union.
- In the symmetric-surd branch, keep irrational boundaries exact; do not decimalise square roots.
- Render all math as LaTeX inside $...$.
- Do NOT state or hint at the answer: do not reveal the roots, sign patterns, individual solution sets, their intersection, or the final interval(s).